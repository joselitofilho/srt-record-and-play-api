export enum EventType {
  CLICK = 'click',
  CHANGE = 'change',
  DBLCLICK = 'dblclick',
  KEYDOWN = 'keydown',
  SUBMIT = 'submit',
}

export enum ControlAction {
  START = 'start',
  STOP = 'stop',
  RESET = 'reset',
  DELETE = 'delete',
  MOVE = 'move',
  PUSH = 'push',
}

export enum RecState {
  ON = 'on',
  OFF = 'off',
  PAUSED = 'paused',
}

export enum MatchPattern {
  CSS = 'css',
  ID = 'id',
  XPATH = 'xpath',
}
